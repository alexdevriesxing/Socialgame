import { mkdir } from 'node:fs/promises';
import { chromium } from 'playwright';

const baseURL=process.env.BASE_URL||'http://127.0.0.1:4173';
const failures=[];await mkdir('test-results',{recursive:true});
function watch(page){page.on('pageerror',error=>failures.push(`pageerror: ${error.stack||error.message}`));page.on('console',message=>{if(message.type()==='error')failures.push(`console.error: ${message.text()}`);});page.on('requestfailed',request=>failures.push(`requestfailed: ${request.method()} ${request.url()} — ${request.failure()?.errorText||'unknown'}`));}
async function waitForGame(page){await page.waitForFunction(()=>document.getElementById('loading')?.classList.contains('hidden')&&typeof game!=='undefined'&&game.mode==='title',null,{timeout:20000});const health=await page.locator('#game').evaluate(canvas=>{const data=canvas.getContext('2d').getImageData(0,0,canvas.width,canvas.height).data;let opaque=0,changes=0,previous=-1;for(let i=0;i<data.length;i+=64){if(data[i+3]>0)opaque++;const light=data[i]+data[i+1]+data[i+2];if(previous>=0&&Math.abs(light-previous)>24)changes++;previous=light;}return{opaque,changes,width:canvas.width,height:canvas.height};});if(health.width!==960||health.height!==540||health.opaque<1000||health.changes<100)throw new Error(`Canvas health failed: ${JSON.stringify(health)}`);const release=await page.evaluate(()=>window.SAKURA_RELEASE);if(release?.version!=='1.8.0')throw new Error(`Unexpected release marker: ${JSON.stringify(release)}`);}
async function artHealth(page,label){const health=await page.locator('#game').evaluate(canvas=>{const data=canvas.getContext('2d').getImageData(0,0,canvas.width,canvas.height).data,buckets=new Set();let transitions=0,previous=-1,opaque=0;for(let i=0;i<data.length;i+=128){if(data[i+3]>0)opaque++;const r=data[i]>>4,g=data[i+1]>>4,b=data[i+2]>>4,l=r+g+b;buckets.add(`${r}:${g}:${b}`);if(previous>=0&&Math.abs(l-previous)>3)transitions++;previous=l;}return{colors:buckets.size,transitions,opaque};});if(health.colors<80||health.transitions<350||health.opaque<2000)throw new Error(`${label} appears visually underdeveloped: ${JSON.stringify(health)}`);}
async function clickCanvas(page,x,y){const box=await page.locator('#game').boundingBox();if(!box)throw new Error('Canvas is not visible.');await page.mouse.click(box.x+x/960*box.width,box.y+y/540*box.height);}
async function clearDialogues(page){let quiet=0;for(let cycle=0;cycle<60;cycle++){if(await page.evaluate(()=>Boolean(game.dialogue))){quiet=0;await page.keyboard.press('Space');await page.waitForTimeout(90);continue;}await page.waitForTimeout(180);if(await page.evaluate(()=>Boolean(game.dialogue))){quiet=0;continue;}quiet++;if(quiet>=3)return;}throw new Error('Opening dialogue sequence did not settle.');}

const browser=await chromium.launch({headless:true});
try{
  const desktop=await browser.newContext({viewport:{width:1440,height:900},reducedMotion:'reduce'});const page=await desktop.newPage();watch(page);await page.goto(baseURL,{waitUntil:'networkidle'});await waitForGame(page);
  const coverage=await page.evaluate(()=>({active:validateActiveSchoolContent(),visual:validateActiveVisualLayout(),polish:validateVisualPolish(),world:validateExpandedWorld(),navigation:validateWorldNavigation(),commercialUI:validateCommercialUI(),commercialCampus:validateCommercialCampus(),walkable:validateWalkableWorld(),commercialWorld:validateCommercialWorldArt(),a11yCore:validateAccessibilityCore(),a11yUI:validateAccessibilityUI(),release:validateReleaseReadiness(),saveVersion:CURRENT_SAVE_VERSION}));
  if(!coverage.active.valid||coverage.active.subjects!==9||coverage.active.clubActivities!==16)throw new Error(`Active School coverage failed: ${JSON.stringify(coverage.active)}`);
  if(!coverage.visual.valid||!coverage.polish.valid||!coverage.world.valid||coverage.world.locations!==13||coverage.world.scenes!==30)throw new Error(`Content/visual coverage failed: ${JSON.stringify(coverage)}`);
  if(!coverage.navigation.valid||coverage.saveVersion!==10||!coverage.commercialUI.valid||!coverage.commercialCampus.valid||coverage.commercialCampus.placeholderMap!==false)throw new Error(`World navigation/commercial validation failed: ${JSON.stringify(coverage)}`);
  if(!coverage.walkable.valid||coverage.walkable.maps!==14||coverage.walkable.placeholderCards!==false||!coverage.commercialWorld.valid||coverage.commercialWorld.placeholderCards!==false)throw new Error(`Walkable art validation failed: ${JSON.stringify(coverage)}`);
  if(!coverage.a11yCore.valid||coverage.a11yCore.themes<13||coverage.a11yCore.audioChannels!==4||coverage.a11yCore.externalAudio!==false||!coverage.a11yUI.valid||coverage.a11yUI.pages!==4||!coverage.release.valid)throw new Error(`Release validation failed: ${JSON.stringify(coverage)}`);
  await artHealth(page,'v1.8 title');await page.screenshot({path:'test-results/title-desktop.png',fullPage:true});

  for(let pageIndex=0;pageIndex<4;pageIndex++){await page.evaluate(index=>openAccessibility(index),pageIndex);await page.waitForFunction(index=>game.overlay?.type==='accessibility'&&game.overlay.page===index,pageIndex);await page.screenshot({path:`test-results/accessibility-${['audio','reading','visual','controls'][pageIndex]}-desktop.png`,fullPage:true});}
  const preferenceWrite=await page.evaluate(()=>{game.settings.musicVolume=.4;game.settings.effectsVolume=.5;game.settings.dialogueVolume=.6;game.settings.textSpeed='fast';game.settings.touchScale='medium';game.settings.highContrast=true;game.settings.reducedMotion=true;game.settings.controlMap.up='i';a11yPersist();return{stored:JSON.parse(localStorage.getItem(A11Y_PREFS_KEY)),dataset:{...document.documentElement.dataset}};});
  if(preferenceWrite.stored.musicVolume!==.4||preferenceWrite.stored.controlMap.up!=='i'||preferenceWrite.dataset.highContrast!=='true')throw new Error(`Preference write failed: ${JSON.stringify(preferenceWrite)}`);
  await page.reload({waitUntil:'networkidle'});await waitForGame(page);const preferenceReload=await page.evaluate(()=>({music:game.settings.musicVolume,effects:game.settings.effectsVolume,dialogue:game.settings.dialogueVolume,speed:game.settings.textSpeed,touch:game.settings.touchScale,up:game.settings.controlMap.up,contrast:document.documentElement.dataset.highContrast,motion:document.documentElement.dataset.reducedMotion}));
  if(preferenceReload.music!==.4||preferenceReload.effects!==.5||preferenceReload.dialogue!==.6||preferenceReload.speed!=='fast'||preferenceReload.touch!=='medium'||preferenceReload.up!=='i'||preferenceReload.contrast!=='true'||preferenceReload.motion!=='true')throw new Error(`Preference reload failed: ${JSON.stringify(preferenceReload)}`);

  await clickCanvas(page,800,155);await page.locator('#creator:not(.hidden)').waitFor({timeout:5000});await page.locator('#nameInput').fill('QA Student');await page.locator('#confirmName').click();await page.waitForFunction(()=>game.mode==='play',null,{timeout:5000});await clearDialogues(page);
  const state=await page.evaluate(()=>({name:game.player.name,year:game.year,month:game.month,wallet:game.player.wallet,customization:Object.keys(game.player.customization||{}).length,routines:validateCampusRoutines(),settings:{music:game.settings.musicVolume,up:game.settings.controlMap.up},release:validateReleaseReadiness()}));
  if(state.name!=='QA Student'||state.year!==1||state.month!==1||state.wallet!==1200||state.customization!==8||!state.routines.valid||state.settings.up!=='i'||!state.release.valid)throw new Error(`New-game v1.8 state failed: ${JSON.stringify(state)}`);
  const recovery=await page.evaluate(()=>{const base=gameSnapshot();writeSave(localStorage,{...base,day:2});writeSave(localStorage,{...base,day:3});localStorage.setItem(SAVE_KEY,'{"corrupt"');const restored=readSave(localStorage);const report=getSaveRecoveryReport();writeSave(localStorage,base);return{day:restored?.day,report,quarantined:Boolean(localStorage.getItem(SAVE_CORRUPT_KEY))};});
  if(recovery.day!==2||!recovery.report.recovered||recovery.report.source!=='sakura-crest-social-summit-backup'||!recovery.quarantined)throw new Error(`Browser save recovery failed: ${JSON.stringify(recovery)}`);
  await artHealth(page,'Release-ready academy');await page.screenshot({path:'test-results/academy-live-desktop.png',fullPage:true});

  const remapBefore=await page.evaluate(()=>({x:game.player.x,y:game.player.y}));await page.keyboard.down('i');await page.waitForTimeout(500);await page.keyboard.up('i');const remapAfter=await page.evaluate(()=>({x:game.player.x,y:game.player.y}));if(Math.hypot(remapAfter.x-remapBefore.x,remapAfter.y-remapBefore.y)<3)throw new Error(`Remapped movement failed: ${JSON.stringify({remapBefore,remapAfter})}`);
  await page.evaluate(()=>{game.overlay=null;openDialogue({speaker:'Accessibility QA',text:'This dialogue verifies readable history, fast text and saved results.',choices:[{text:'Record this exchange.',result:'The exchange was recorded successfully.'}]});game.dialogue.reveal=game.dialogue.text.length;closeDialogue(0);openDialogueHistory();});await page.waitForFunction(()=>game.overlay?.type==='dialogueHistory');const history=await page.evaluate(()=>game.dialogueHistory.find(entry=>entry.speaker==='Accessibility QA'));if(history?.result!=='The exchange was recorded successfully.')throw new Error(`Dialogue history failed: ${JSON.stringify(history)}`);await page.screenshot({path:'test-results/dialogue-history-desktop.png',fullPage:true});
  await page.evaluate(()=>{game.overlay=null;game.settings.performanceMode=false;game.settings.autoPerformance=true;for(let index=0;index<180;index++)update(.04);openPerformanceReport();});await page.waitForFunction(()=>game.overlay?.type==='performanceReport');const performanceState=await page.evaluate(()=>({mode:game.settings.performanceMode,stats:game.performanceStats,dataset:document.documentElement.dataset.performanceMode}));if(!performanceState.mode||performanceState.stats.samples<180||performanceState.dataset!=='true')throw new Error(`Performance safeguard failed: ${JSON.stringify(performanceState)}`);await page.screenshot({path:'test-results/performance-report-desktop.png',fullPage:true});

  await page.evaluate(()=>{game.settings.performanceMode=false;game.settings.autoPerformance=false;a11yPersist();game.overlay=null;game.dialogue=null;game.time=850;openWorldMap({weekend:true});});await page.waitForFunction(()=>game.overlay?.type==='worldMap');await artHealth(page,'District map');await page.screenshot({path:'test-results/district-map-desktop.png',fullPage:true});await page.evaluate(()=>visitWorldLocation('cafe',{weekend:true}));await page.waitForFunction(()=>game.walkable?.active&&game.walkable.locationId==='cafe'&&!game.overlay);await artHealth(page,'Walkable cafe');
  const moved=await page.evaluate(()=>{const s=wwEnsureState(),before={x:s.x,y:s.y};keys.add('arrowleft');for(let i=0;i<4;i++)wwUpdate(.15);keys.delete('arrowleft');return{before,after:{x:s.x,y:s.y},npcs:s.npcs.length};});if(Math.hypot(moved.after.x-moved.before.x,moved.after.y-moved.before.y)<4||moved.npcs<1)throw new Error(`Walkable cafe failed: ${JSON.stringify(moved)}`);
  const mapIds=['home','shopping','cafe','arcade','park','public_library','cinema','sports_center','music_venue','festival','museum','study_center','convenience','station'];for(const id of mapIds){await page.evaluate(locationId=>wwEnter(locationId,{weekend:true,preserve:true}),id);await page.waitForFunction(locationId=>game.walkable?.active&&game.walkable.locationId===locationId,id);await artHealth(page,`Walkable ${id}`);await page.screenshot({path:`test-results/walkable-${id}.png`,fullPage:true});}

  await page.waitForFunction(async()=>{if(!('serviceWorker'in navigator))return false;await navigator.serviceWorker.ready;return true;},null,{timeout:15000});await page.reload({waitUntil:'networkidle'});await waitForGame(page);await page.waitForFunction(()=>Boolean(navigator.serviceWorker.controller),null,{timeout:10000});await desktop.setOffline(true);await page.reload({waitUntil:'domcontentloaded',timeout:15000});await waitForGame(page);await page.screenshot({path:'test-results/offline-desktop.png',fullPage:true});await desktop.setOffline(false);await desktop.close();

  const tablet=await browser.newContext({viewport:{width:1024,height:768},hasTouch:true,isMobile:true,deviceScaleFactor:1});const tabletPage=await tablet.newPage();watch(tabletPage);await tabletPage.goto(baseURL,{waitUntil:'networkidle'});await waitForGame(tabletPage);await tabletPage.evaluate(()=>{resetGame();game.mode='play';game.dialogue=null;game.overlay=null;game.showControls=true;});await tabletPage.waitForTimeout(250);const tabletControls=await tabletPage.evaluate(()=>buttons.filter(button=>button.holdKey).map(button=>({w:button.w,h:button.h,key:button.holdKey})));if(tabletControls.length<4||tabletControls.some(button=>button.w<32||button.h<32))throw new Error(`Tablet touch controls invalid: ${JSON.stringify(tabletControls)}`);await tabletPage.screenshot({path:'test-results/tablet-touch-controls.png',fullPage:true});await tablet.close();
  const landscape=await browser.newContext({viewport:{width:844,height:390},hasTouch:true,isMobile:true,deviceScaleFactor:2});const landscapePage=await landscape.newPage();watch(landscapePage);await landscapePage.goto(baseURL,{waitUntil:'networkidle'});await waitForGame(landscapePage);await landscapePage.evaluate(()=>{resetGame();game.mode='play';game.dialogue=null;game.overlay=null;game.showControls=true;game.settings.touchScale='large';});await landscapePage.waitForTimeout(250);const appBox=await landscapePage.locator('#app').boundingBox();if(!appBox||appBox.width>845||appBox.height>391)throw new Error(`Landscape layout escaped viewport: ${JSON.stringify(appBox)}`);await landscapePage.screenshot({path:'test-results/mobile-landscape-touch.png',fullPage:true});await landscape.close();
  const portrait=await browser.newContext({viewport:{width:390,height:844},hasTouch:true,isMobile:true,deviceScaleFactor:2});const portraitPage=await portrait.newPage();watch(portraitPage);await portraitPage.goto(baseURL,{waitUntil:'networkidle'});await waitForGame(portraitPage);const orientation=await portraitPage.locator('#orientation-hint').evaluate(element=>({display:getComputedStyle(element).display,text:element.textContent}));if(orientation.display==='none'||!orientation.text.includes('Rotate your device'))throw new Error(`Portrait orientation guidance failed: ${JSON.stringify(orientation)}`);await portraitPage.screenshot({path:'test-results/mobile-portrait-guidance.png',fullPage:true});await portrait.close();
}finally{await browser.close();}
if(failures.length)throw new Error(`Browser QA captured ${failures.length} failure(s):\n${failures.join('\n')}`);
console.log('v1.8 browser QA passed: resilient v10 save recovery, balanced release validation, original audio, persistent accessibility, remapped movement, desktop/tablet/phone UX, all walkable maps and offline reload.');