import { readFile } from 'node:fs/promises';
import vm from 'node:vm';

const listeners={};
class Element{
  constructor(id){this.id=id;this.classList={_s:new Set(),add(v){this._s.add(v)},remove(v){this._s.delete(v)},contains(v){return this._s.has(v)}};this.value='Akira';this.textContent='';this.dataset={};this.style={};this.children=[];}
  addEventListener(type,fn){(listeners[`${this.id}:${type}`]??=[]).push(fn)}setAttribute(){}append(...nodes){this.children.push(...nodes)}appendChild(node){this.children.push(node);return node;}
}
const ctx=new Proxy({imageSmoothingEnabled:true,textAlign:'left',textBaseline:'alphabetic',measureText(t){return{width:String(t).length*8}},createLinearGradient(){return{addColorStop(){}}}}, {get(o,p){if(p in o)return o[p];return()=>{}},set(o,p,v){o[p]=v;return true}});
const canvas=new Element('game');canvas.width=960;canvas.height=540;canvas.getContext=()=>ctx;canvas.getBoundingClientRect=()=>({left:0,top:0,width:960,height:540});
const elements={game:canvas,creator:new Element('creator'),nameInput:new Element('nameInput'),confirmName:new Element('confirmName'),loading:new Element('loading'),orientationHint:new Element('orientation-hint')};elements.creator.classList.add('hidden');
const documentElement=new Element('html'),body=new Element('body');globalThis.document={documentElement,body,getElementById:id=>elements[id],createElement:tag=>new Element(tag)};globalThis.window=globalThis;window.addEventListener=(t,f)=>{(listeners[`window:${t}`]??=[]).push(f)};window.dispatchEvent=()=>true;window.matchMedia=()=>({matches:false});
globalThis.CustomEvent=class{constructor(type,options={}){this.type=type;this.detail=options.detail;}};globalThis.location={protocol:'file:',reload(){}};
window.AudioContext=class{constructor(){this.currentTime=0;this.state='running';this.destination={}}createOscillator(){return{type:'',frequency:{value:0},connect(){},start(){},stop(){}}}createGain(){return{gain:{value:0,setValueAtTime(){},exponentialRampToValueAtTime(){}},connect(){}}}resume(){}};
Object.defineProperty(globalThis,'navigator',{value:{maxTouchPoints:0,storage:{persist:async()=>true}},configurable:true});
globalThis.localStorage={_d:{},getItem(k){return this._d[k]??null},setItem(k,v){this._d[k]=String(v)},removeItem(k){delete this._d[k]}};
globalThis.requestAnimationFrame=()=>0;globalThis.performance={now:()=>1000};globalThis.setInterval=()=>1;globalThis.clearInterval=()=>{};globalThis.Image=class{set src(v){this._src=v;queueMicrotask(()=>this.onload?.())}};

const index=await readFile('index.html','utf8');
const scripts=[...index.matchAll(/<script src="([^"]+)"><\/script>/g)].map(match=>match[1]);
if(scripts.length<32)throw new Error(`Expected at least 32 production scripts; found ${scripts.length}.`);
for(const script of scripts)vm.runInThisContext(await readFile(script,'utf8'),{filename:script});
await new Promise(resolve=>setTimeout(resolve,25));
if(!elements.loading.classList.contains('hidden'))throw new Error('Static game did not complete initialization.');
const checks=[
  [CAMPAIGN_MONTHS.length,48,'campaign chapters'],[Object.values(RELATIONSHIP_ROUTES).flat().length,30,'friendship chapters'],
  [PHONE_MESSAGES.length,20,'phone conversations'],[RIVALRY_SCENES.length,20,'rivalry chapters'],[MONTHLY_SHOWCASES.length,12,'showcases'],
  [CAMPUS_ACTIVITIES.length,8,'campus activities'],[ACHIEVEMENTS.length,40,'achievements'],[FRIENDSHIP_MOMENTS.length,10,'illustrated moments'],
  [CAMPUS_INCIDENTS.length,24,'campus incidents'],[GRADUATION_LEGACIES.length,12,'graduation legacies'],[LEADERSHIP_ELECTIONS.length,4,'elections']
];
for(const [actual,expected,label] of checks)if(actual!==expected)throw new Error(`Expected ${expected} ${label}; got ${actual}.`);
ensureV10State();ensureActiveSchoolState();ensureWorldState();wwEnsureState();
game.player.gender='boy';game.player.routeScenes=['ren-1','ren-2','ren-3'];game.player.relationships.ren=15;
const ren=game.npcs.find(n=>n.id==='ren');if(!nextFriendshipMoment(ren))throw new Error('Illustrated friendship moment did not unlock.');
game.time=855;game.dayFlags.activity=false;const activity=CAMPUS_ACTIVITIES.find(a=>a.mechanic==='focus');startCampusMastery(activity);for(let i=0;i<4;i++){game.overlay.position=game.overlay.targetCenter;campusMasteryAction();}
if(game.overlay.phase!=='result'||game.player.campusBest[activity.id]<90)throw new Error('Campus mastery minigame did not record an S-level result.');
const legacy=determineGraduationLegacy(false);if(!legacy?.name)throw new Error('Graduation legacy calculation failed.');const art=createArtSources();if(!art.memory_atlas||!art.character_atlas)throw new Error('Scalable art sources are incomplete.');
game.overlay=null;game.year=1;game.month=9;game.day=17;game.player.electionHistory=[];game.player.leadershipWins=0;if(!electionDue()||!startElection()||game.overlay.type!=='election')throw new Error('Election flow did not start.');
chooseCampaignPlatform('community');for(let i=0;i<3;i++){electionAnswer(game.overlay.cfg.questions[game.overlay.questionIndex].correct);nextElectionQuestion();}if(game.player.electionHistory.length!==1||game.player.campaignBest<=0)throw new Error('Election result was not recorded.');game.overlay=null;openLeadershipJournal();if(game.overlay?.type!=='leadership')throw new Error('Leadership journal did not open.');game.overlay=null;

const routineValidation=validateCampusRoutines();if(!routineValidation.valid)throw new Error(`Living Campus routines invalid: ${routineValidation.errors.join(', ')}`);if(routineValidation.studentPeriods!==110||routineValidation.teacherPeriods!==44)throw new Error(`Unexpected routine coverage: ${JSON.stringify(routineValidation)}`);
game.mode='play';game.dialogue=null;game.overlay=null;game.paused=false;game.time=510;game.dailyBrief={id:`${game.year}-${game.month}-${game.day}`,weather:'Clear',notice:'QA notice'};relocateNPCs('math');
for(const actor of [...game.npcs,...game.teachers]){if(!actor.campus?.room||!ROOMS[actor.campus.room])throw new Error(`${actor.id} has no valid living-campus destination.`);if(!Number.isFinite(actor.x)||!Number.isFinite(actor.y))throw new Error(`${actor.id} has invalid coordinates.`);}
for(let step=0;step<180;step++)updateCampusSimulation(.25);for(const actor of [...game.npcs,...game.teachers])if(!Number.isFinite(actor.x)||!Number.isFinite(actor.y))throw new Error(`${actor.id} movement became non-finite.`);
game.dailyBrief={id:`${game.year}-${game.month}-${game.day}`,weather:'Heavy Rain',notice:'Courtyard closed'};game.time=620;game.dayFlags.incident=false;campusApplyRoutines({key:'break',slotId:'break',transition:false,slot:SCHEDULE.find(slot=>slot.id==='break')},{instant:true});if(campusRoomOpen('courtyard'))throw new Error('Rain closure did not close the courtyard.');if(game.npcs.some(npc=>npc.campus?.room==='courtyard'))throw new Error('A student remained routed to the closed courtyard.');openLivingCampusOverlay();if(game.overlay?.type!=='livingCampus')throw new Error('Campus Pulse overlay did not open.');game.overlay=null;

const activeValidation=validateActiveSchoolContent();if(!activeValidation.valid)throw new Error(`Active School content invalid: ${activeValidation.issues.join(', ')}`);if(activeValidation.subjects!==9||activeValidation.clubActivities!==16||activeValidation.seasonalEvents!==16)throw new Error(`Unexpected Active School coverage: ${JSON.stringify(activeValidation)}`);
const visualValidation=validateActiveVisualLayout();if(!visualValidation.valid)throw new Error(`Visual layout invalid: ${visualValidation.issues.join(', ')}`);const polishValidation=validateVisualPolish();if(!polishValidation.valid)throw new Error(`Visual polish invalid: ${polishValidation.issues.join(', ')}`);
game.overlay=null;game.time=620;startActiveSchoolActivity('math',{practice:true});activeBegin();for(let round=0;round<4;round++){const question=game.overlay.questions[game.overlay.round];activeDecisionAnswer(question.correct);activeNextDecision();}if(game.overlay.phase!=='result'||game.player.activityBest.math!==100||game.player.subjectMastery.math<1)throw new Error('Mathematics decision activity did not persist a perfect result.');
game.overlay=null;startActiveSchoolActivity('literature',{practice:true});activeBegin();game.overlay.preview=0;for(const symbol of [...game.overlay.sequence])activeSequenceInput(ACTIVE_SYMBOLS.indexOf(symbol));if(game.overlay.phase!=='result'||game.player.activityBest.literature!==100)throw new Error('Literature sequence activity failed.');
game.overlay=null;startActiveSchoolActivity('science',{practice:true});activeBegin();for(let round=0;round<4;round++){game.overlay.position=game.overlay.targetCenter;activeTimingHit();}if(game.overlay.phase!=='result'||game.player.activityBest.science!==100)throw new Error('Science timing activity failed.');
game.overlay=null;startActiveSchoolActivity('art',{practice:true});const widthBefore=game.overlay.targetWidth;activeToggleAssist();if(game.overlay.targetWidth===widthBefore||game.overlay.assist!==game.player.activityAssist)throw new Error('Activity assist did not update the active challenge.');activeBegin();finishActiveSchool(100);if(game.overlay.phase!=='result'||game.player.activityBest.art!==100)throw new Error('Art balance activity failed.');
game.overlay=null;game.player.club='council';game.time=745;const seasonalKey=activeSeasonalKey();startActiveSchoolActivity('council-debate',{competition:true});activeBegin();for(let round=0;round<4;round++){game.overlay.position=game.overlay.targetCenter;activeTimingHit();}if(game.overlay.phase!=='result'||!game.player.seasonalCompetitionWins.includes(seasonalKey)||game.player.clubWins<1)throw new Error('Seasonal club competition result was not recorded.');

const worldValidation=validateExpandedWorld();if(!worldValidation.valid)throw new Error(`Expanded World content invalid: ${worldValidation.issues.join(', ')}`);if(worldValidation.locations!==13||worldValidation.profiles!==10||worldValidation.scenes!==30||worldValidation.customizationCategories!==8)throw new Error(`Unexpected Expanded World coverage: ${JSON.stringify(worldValidation)}`);
for(const npc of NPCS){const profile=profileForNpc(npc.id);if(!profile?.birthday||profile.interests.length<3||profile.likes.length<3||profile.dislikes.length<2||profile.topics.length<3||profile.scenes.length!==3)throw new Error(`${npc.id} preference profile is incomplete.`);}
const migrated=migrateSave({version:8,data:{player:{},dayFlags:{}}});if(migrated.version!==10||migrated.data.player.wallet!==1200||Object.keys(migrated.data.player.customization).length!==8||!migrated.data.releaseStats)throw new Error('v8 to v10 save migration failed.');

game.overlay=null;game.dialogue=null;game.mode='play';game.time=850;game.player.collectibles=[];game.player.collectionDates={};const firstCollectible=grantCollectible('badge-explorer','QA unlock');const duplicateCollectible=grantCollectible('badge-explorer','QA duplicate');if(!firstCollectible||duplicateCollectible||game.player.collectibles.filter(id=>id==='badge-explorer').length!==1)throw new Error('Duplicate-safe collection gate failed.');
game.player.wallet=5000;if(!buyOrEquipCustomization('hair','sakura')||game.player.customization.hair!=='sakura'||!game.player.ownedCustomization.hair.includes('sakura'))throw new Error('Customization purchase/equip failed.');writeSave(localStorage,gameSnapshot());const persisted=readSave(localStorage);if(persisted.player.customization.hair!=='sakura'||persisted.player.wallet!==game.player.wallet)throw new Error('Customization did not persist across save/load.');
game.player.giftInventory['sports-towel']=1;game.month=2;game.day=14;game.player.relationships.mina=0;if(giftReaction(profileForNpc('mina'),giftById('sports-towel'))!=='loved')throw new Error('Liked gift matching failed.');if(giftReaction(profileForNpc('rina'),giftById('sports-towel'))!=='disliked')throw new Error('Disliked gift matching failed.');giveGift('mina','sports-towel');if(game.player.relationships.mina!==5||!game.player.giftHistory.at(-1)?.birthday)throw new Error('Birthday gift bonus was not applied.');game.dialogue=null;
const visitsBefore={...game.player.worldVisits},timeBefore=game.time;game.overlay={type:'giftShop',returnLocation:'cafe',returnWeekend:true};const navigationValidation=validateWorldNavigation();if(!navigationValidation.valid||game.time!==timeBefore||JSON.stringify(game.player.worldVisits)!==JSON.stringify(visitsBefore))throw new Error(`World nested navigation failed: ${navigationValidation.issues.join(', ')}`);
const commercialUI=validateCommercialUI();if(!commercialUI.valid||!commercialUI.roundedPanels||!commercialUI.modalSafeHud)throw new Error(`Commercial UI invalid: ${JSON.stringify(commercialUI)}`);const commercialCampus=validateCommercialCampus();if(!commercialCampus.valid||commercialCampus.placeholderMap!==false||commercialCampus.illustratedRooms!==8)throw new Error(`Commercial campus invalid: ${JSON.stringify(commercialCampus)}`);const walkableValidation=validateWalkableWorld();if(!walkableValidation.valid||walkableValidation.maps!==14||walkableValidation.walkableOffsite!==13||walkableValidation.placeholderCards!==false)throw new Error(`Walkable world invalid: ${JSON.stringify(walkableValidation)}`);const commercialWorld=validateCommercialWorldArt();if(!commercialWorld.valid||commercialWorld.placeholderCards!==false||!commercialWorld.illustratedDistrict)throw new Error(`Commercial world art invalid: ${JSON.stringify(commercialWorld)}`);
const accessibilityCore=validateAccessibilityCore();if(!accessibilityCore.valid||accessibilityCore.themes<13||accessibilityCore.audioChannels!==4||accessibilityCore.externalAudio!==false)throw new Error(`Accessibility/audio core invalid: ${JSON.stringify(accessibilityCore)}`);const accessibilityUI=validateAccessibilityUI();if(!accessibilityUI.valid||accessibilityUI.pages!==4||!accessibilityUI.remappingUI)throw new Error(`Accessibility UI invalid: ${JSON.stringify(accessibilityUI)}`);
const releaseValidation=validateReleaseReadiness();if(!releaseValidation.valid||releaseValidation.knownCriticalIssues!==0||releaseValidation.knownHighIssues!==0)throw new Error(`Release readiness invalid: ${JSON.stringify(releaseValidation)}`);
game.settings.musicVolume=.4;game.settings.effectsVolume=.5;game.settings.dialogueVolume=.6;game.settings.textSpeed='fast';game.settings.touchScale='medium';game.settings.controlMap.up='i';a11yPersist();const savedPrefs=JSON.parse(localStorage.getItem(A11Y_PREFS_KEY));if(savedPrefs.musicVolume!==.4||savedPrefs.dialogueVolume!==.6||savedPrefs.controlMap.up!=='i')throw new Error('Accessibility preferences did not persist.');
const historyBefore=game.dialogueHistory.length;openDialogue({speaker:'QA Narrator',text:'Accessible dialogue history test.',choices:[{text:'Continue',result:'History result recorded.'}]});game.dialogue.reveal=game.dialogue.text.length;closeDialogue(0);if(game.dialogueHistory.length!==historyBefore+1||game.dialogueHistory.at(-1)?.result!=='History result recorded.')throw new Error('Dialogue history did not record the completed exchange.');openAccessibility(3);if(game.overlay?.type!=='accessibility'||game.overlay.page!==3)throw new Error('Accessibility controls page did not open.');game.overlay={type:'performanceReport'};game.settings.performanceMode=false;game.settings.autoPerformance=true;for(let i=0;i<180;i++)update(.04);if(!game.settings.performanceMode||!game.performanceStats||game.performanceStats.samples<180)throw new Error('Automatic low-power mode did not activate under sustained slow frames.');

game.overlay=null;openWorldMap({weekend:true});if(game.overlay?.type!=='worldMap')throw new Error('Illustrated district map did not open.');visitWorldLocation('cafe',{weekend:true});const walk=wwEnsureState();if(!walk.active||walk.locationId!=='cafe'||game.overlay!==null)throw new Error('Café did not open as a walkable map.');const beforeMove=walk.x;keys.add('arrowleft');wwUpdate(.25);keys.delete('arrowleft');if(!(walk.x<beforeMove)&&!wwCollides(beforeMove-31,walk.y))throw new Error('Walkable player movement did not advance.');walk.x=440;walk.y=300;keys.add('arrowright');wwUpdate(.25);keys.delete('arrowright');if(walk.x!==440)throw new Error('Walkable furniture collision did not block movement.');if(walk.npcs.length){const npc=walk.npcs[0],npcStart=npc.x;npc.targetX=npc.x+40;npc.targetY=npc.y;npc.wait=0;for(let i=0;i<8;i++)wwUpdate(.25);if(!Number.isFinite(npc.x)||npc.x===npcStart)throw new Error('Walkable NPC wandering did not advance.');}for(const id of Object.keys(WW_SCENES)){wwEnter(id,{weekend:true,preserve:true});const state=wwEnsureState();if(!state.active||state.locationId!==id||wwCollides(state.x,state.y))throw new Error(`${id} walkable map has an invalid spawn.`);wwDrawScene();}openHomeHub();if(!wwEnsureState().active||wwEnsureState().locationId!=='home')throw new Error('Bedroom did not open as a walkable map.');openCollectionBook();if(game.overlay?.type!=='collectionBook'||!wwEnsureState().active)throw new Error('Collection book did not preserve the walkable bedroom context.');
if(window.SAKURA_RELEASE?.version!=='1.8.0')throw new Error(`Unexpected release marker ${window.SAKURA_RELEASE?.version}.`);if(CURRENT_SAVE_VERSION!==10)throw new Error(`Unexpected save version ${CURRENT_SAVE_VERSION}.`);
const animeArtValidation=validateAnimeArtV18();if(!animeArtValidation.valid||animeArtValidation.externalRuntimeDependency!==false||animeArtValidation.proceduralFallbacks!==false||animeArtValidation.characterCount!==16||animeArtValidation.campusWings!==3)throw new Error(`Anime art integration invalid: ${JSON.stringify(animeArtValidation)}`);
console.log(`Remote runtime smoke passed for ${scripts.length} scripts, ${routineValidation.studentPeriods+routineValidation.teacherPeriods} campus routines, ${activeValidation.subjects} subjects, ${walkableValidation.maps} walkable maps, ${accessibilityCore.themes} original themes, permanent v1.8 anime artwork, resilient v10 saves, balanced rankings and zero placeholder screens.`);
