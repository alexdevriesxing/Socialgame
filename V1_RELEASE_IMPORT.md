# Leadership & Legacy v1.0 release import

This temporary marker initializes the release branch. The validated Cloudflare package is imported by GitHub Actions from SHA-verified pull-request payload chunks and replaces this marker automatically.
